</main>
</div>

<style>
.master-header {
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  color: white;
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  z-index: 1000;
  height: 60px;
}

.master-navbar {
  height: 60px;
  display: flex;
  align-items: center;
}

.master-layout {
  display: flex;
  margin-top: 60px;
  min-height: calc(100vh - 60px);
}

.master-sidebar {
  width: 250px;
  background: linear-gradient(180deg, #667eea 0%, #764ba2 100%);
  color: white;
  position: fixed;
  top: 60px;
  left: 0;
  bottom: 0;
  overflow-y: auto;
}

.master-main {
  margin-left: 250px;
  padding: 20px;
  flex: 1;
  background: #f8f9fa;
  min-height: calc(100vh - 60px);
}

.master-sidebar .sidebar-nav {
  list-style: none;
  padding: 0;
  margin: 0;
}

.master-sidebar .sidebar-nav .nav-item {
  border-bottom: 1px solid rgba(255,255,255,0.1);
}

.master-sidebar .sidebar-nav .nav-link {
  display: flex;
  align-items: center;
  padding: 15px 20px;
  color: rgba(255,255,255,0.8);
  text-decoration: none;
  transition: all 0.3s ease;
}

.master-sidebar .sidebar-nav .nav-link:hover,
.master-sidebar .sidebar-nav .nav-link.active {
  background: rgba(255,255,255,0.1);
  color: white;
}

.master-sidebar .sidebar-nav .nav-link i {
  margin-right: 10px;
  width: 20px;
}

.master-nav-right {
  margin-left: auto;
}

.master-nav-right .user-info {
  display: flex;
  align-items: center;
  gap: 15px;
}

.master-nav-right .user-name {
  color: rgba(255,255,255,0.8);
}

@media (max-width: 768px) {
  .master-sidebar {
    transform: translateX(-100%);
    transition: transform 0.3s ease;
  }
  
  .master-main {
    margin-left: 0;
  }
  
  .master-sidebar.open {
    transform: translateX(0);
  }
}
</style>

<script src="https://kit.fontawesome.com/your-fontawesome-kit.js"></script>

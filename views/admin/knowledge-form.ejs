<!DOCTYPE html>
<html lang="uz">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= article._id ? 'Tahrirlash' : 'Qo‘shish' %> Bilimlar Bazasi Maqolasi - Dern-Yordam Admin</title>
  <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
  <%- include('../partials/header') %>
  
  <main class="knowledge-form-page">
    <div class="container">
      <div class="page-header">
        <h1><%= article._id ? 'Tahrirlash' : 'Qo‘shish' %> Bilimlar Bazasi Maqolasi</h1>
        <a href="/knowledge/admin" class="btn btn-secondary btn-sm">Bilimlar Bazasiga Qaytish</a>
      </div>
      
      <% if (typeof error !== 'undefined') { %>
        <div class="alert alert-error">
          <%= error %>
        </div>
      <% } %>
      
      <div class="form-container">
        <form action="<%= article._id ? '/knowledge/admin/' + article._id : '/knowledge/admin' %>" method="POST" class="knowledge-form">
          <div class="form-section">
            <h2>Maqola Ma'lumotlari</h2>
            
            <div class="form-group">
              <label for="title">Sarlavha</label>
              <input 
                type="text" 
                id="title" 
                name="title" 
                required 
                value="<%= article.title || '' %>"
              >
            </div>
            
            <div class="form-row">
              <div class="form-group">
                <label for="category">Kategoriya</label>
                <select id="category" name="category" required>
                  <option value="" disabled <%= !article.category ? 'selected' : '' %>>Kategoriyani tanlang</option>
                  <option value="hardware" <%= article.category === 'hardware' ? 'selected' : '' %>>Uskuna</option>
                  <option value="software" <%= article.category === 'software' ? 'selected' : '' %>>Dasturiy Ta'minot</option>
                  <option value="network" <%= article.category === 'network' ? 'selected' : '' %>>Tarmoq</option>
                  <option value="security" <%= article.category === 'security' ? 'selected' : '' %>>Xavfsizlik</option>
                  <option value="maintenance" <%= article.category === 'maintenance' ? 'selected' : '' %>>Texnik Xizmat</option>
                  <option value="troubleshooting" <%= article.category === 'troubleshooting' ? 'selected' : '' %>>Muammolarni Hal Qilish</option>
                  <option value="other" <%= article.category === 'other' ? 'selected' : '' %>>Boshqa</option>
                </select>
              </div>
              
              <div class="form-group">
                <label>Qurilma Turlari</label>
                <div class="checkbox-group">
                  <label class="checkbox-label">
                    <input 
                      type="checkbox" 
                      name="deviceTypes" 
                      value="desktop" 
                      <%= article.deviceTypes && article.deviceTypes.includes('desktop') ? 'checked' : '' %>
                    >
                    Ish stoli kompyuteri
                  </label>
                  <label class="checkbox-label">
                    <input 
                      type="checkbox" 
                      name="deviceTypes" 
                      value="laptop" 
                      <%= article.deviceTypes && article.deviceTypes.includes('laptop') ? 'checked' : '' %>
                    >
                    Noutbuk
                  </label>
                  <label class="checkbox-label">
                    <input 
                      type="checkbox" 
                      name="deviceTypes" 
                      value="tablet" 
                      <%= article.deviceTypes && article.deviceTypes.includes('tablet') ? 'checked' : '' %>
                    >
                    Planshet
                  </label>
                  <label class="checkbox-label">
                    <input 
                      type="checkbox" 
                      name="deviceTypes" 
                      value="smartphone" 
                      <%= article.deviceTypes && article.deviceTypes.includes('smartphone') ? 'checked' : '' %>
                    >
                    Smartfon
                  </label>
                  <label class="checkbox-label">
                    <input 
                      type="checkbox" 
                      name="deviceTypes" 
                      value="printer" 
                      <%= article.deviceTypes && article.deviceTypes.includes('printer') ? 'checked' : '' %>
                    >
                    Printer
                  </label>
                  <label class="checkbox-label">
                    <input 
                      type="checkbox" 
                      name="deviceTypes" 
                      value="server" 
                      <%= article.deviceTypes && article.deviceTypes.includes('server') ? 'checked' : '' %>
                    >
                    Server
                  </label>
                  <label class="checkbox-label">
                    <input 
                      type="checkbox" 
                      name="deviceTypes" 
                      value="network" 
                      <%= article.deviceTypes && article.deviceTypes.includes('network') ? 'checked' : '' %>
                    >
                    Tarmoq
                  </label>
                  <label class="checkbox-label">
                    <input 
                      type="checkbox" 
                      name="deviceTypes" 
                      value="other" 
                      <%= article.deviceTypes && article.deviceTypes.includes('other') ? 'checked' : '' %>
                    >
                    Boshqa
                  </label>
                </div>
              </div>
            </div>
            
            <div class="form-group">
              <label for="tags">Teglar (vergul bilan ajratilgan)</label>
              <input 
                type="text" 
                id="tags" 
                name="tags" 
                value="<%= article.tags ? article.tags.join(', ') : '' %>"
                placeholder="masalan, wifi, router, ulanish, muammolarni hal qilish"
              >
            </div>
            
            <div class="form-group">
              <label for="content">Mazmun</label>
              <textarea 
                id="content" 
                name="content" 
                rows="20" 
                required
              ><%= article.content || '' %></textarea>
              <p class="form-help">Siz boy matn uchun Markdown formatidan foydalanishingiz mumkin.</p>
            </div>
            
            <div class="form-group">
              <label class="checkbox-label">
                <input 
                  type="checkbox" 
                  id="isPublished" 
                  name="isPublished" 
                  <%= article.isPublished !== false ? 'checked' : '' %>
                >
                Ushbu maqolani nashr qilish
              </label>
            </div>
          </div>
          
          <div class="form-actions">
            <button type="submit" class="btn btn-primary">
              <%= article._id ? 'Yangilash' : 'Yaratish' %> Maqola
            </button>
            <a href="/knowledge/admin" class="btn btn-secondary">Bekor qilish</a>
          </div>
        </form>
      </div>
    </div>
  </main>
  
  <script src="/js/main.js"></script>
</body>
</html>